services:
  bind9:
    image: ubuntu/bind9:latest
    restart: unless-stopped
    container_name: bind9
    environment:
      - BIND9_USER=root
      - TZ=UTC
    volumes:
      - ./config:/etc/bind:ro
      - ~/Documents/homelab-data/bind9/cache:/var/cache/bind
    ports:
      - "53:53/tcp"
      - "53:53/udp"
